<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newlyeaten="{{devour}}">
		{{#if devour}}Devour!{{else}}Make Another{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">Delete Burger</button> --}}
</li>
